# Sprint v6.8 — Canonização do padrão auth Percus

**Data:** 2026-05-19
**Autor:** sessão Opus 4.7 + operador
**Status:** design aprovado, próximo passo `writing-plans`
**Versão canon alvo:** `6.7.2` → `6.8.0` (breaking)

---

## 1. Contexto

A auditoria solicitada pelo operador (3 Explore agents em paralelo + cruzamento com auth-service `D:/Claud Automations/auth-service`) revelou **3 frentes desalinhadas** entre o canon Percus (`_Novo_Projeto/`) e o auth-service de produção:

1. **5 doc gaps** no canon vs implementação real:
   - Audience naming kebab-case não documentado em R7 (incidente 2026-05-19 Plexco Tasks: `plexco_tickets` vs `plexco-tickets`)
   - JWT claim `iid` mencionado em incidentes mas não definido (`sub` vs `iid` ambíguo)
   - S2S pattern (`X-Internal-Auth` header + Docker Secret) implementado mas sem spec oficial
   - SSO multi-domain só 1 parágrafo em `02_INFRA:2.7` — falta TTL, CSRF prevention, Origin/Referer validation
   - PII sanitization R14 (SHA-256 hash) sem verificação cruzada na implementação

2. **`COMANDO_PROJETO_NOVO.md` parcial, `UPGRADE_PARA_FASE6.md` mudo** sobre auth. `MIGRATION_KIT_AUTH.template.md` existe mas não é importado automaticamente pelos comandos.

3. **Sem template canônico de login UI** em `_Novo_Projeto/templates/login-ui/` (agendado em `PADRAO_AUTH_SERVICE_INTEGRATION_V2.md:446` mas não criado). Cada projeto reimplementa. Print do operador mostra o padrão visual desejado (Plexco Coach: toggle WhatsApp/Email + seletor país BR/US/PT/AR + máscara `(DDD) NNNNN-NNNN` + helper text + CTA).

Adicional: commit `8d2cd56` (2026-05-19) cravou o contrato JWT (E.164 com `+`) vs DB (digits-only sem `+`) e a lib `percus-auth 0.3.0` com `phone_handle_for_db_lookup()`. R15 canon foi atualizado em [01_REGRAS_INEGOCIAVEIS.md:427-431](../../../01_REGRAS_INEGOCIAVEIS.md#L427-L431), mas os templates ainda não reforçam.

**Outcome esperado:** projetos novos e existentes podem ser inicializados/atualizados pelo canon com auth Percus canônico de ponta a ponta — script para o mecânico, checklist humano para o que exige decisão/segurança. Audiences uniformes em kebab-case. Login UI parametrizado por tenant via endpoint público do auth-service.

---

## 2. Decisões aprovadas (council + operador)

### 2.1 Tenant detection pré-autenticação → **Opção C**

Council 3/3 (DeepSeek + Llama + Cross-Claude Opus). Operador aprovou.

**Mecanismo:** novo endpoint público no auth-service:

```
GET /tenants/by-origin?origin=<window.location.origin>
→ 200 { audience, product_name, logo_url, logo_url_dark, palette, copy, support_contact_url }
→ 404 { error: "tenant_not_found" }
```

- **Rate limit:** 100 req/min por origin (não por IP — origin é público e CDN cacheia).
- **Cache frontend:** sessionStorage 5min (mitigar LCP impact).
- **Fallback:** se request falha (auth-service down), usa `PERCUS_AUTH_AUDIENCE_FALLBACK` env e renderiza login genérico ("Entrar").
- **Sem enumeration risk** (diferente de `/tenants/by-destination` que foi rejeitado).

### 2.2 Acionamento dos comandos → **Opção 3 (Híbrido)**

Council 2/3 (DeepSeek + Cross-Claude) + Llama divergente reconhecendo o gap.

**Script** (`tools/scaffold-percus-project.ps1`) automatiza só o mecânico (idempotente, sem segredos):
- copia `_Novo_Projeto/templates/login-ui/` → `<projeto>/src/app/login/`
- copia `.env.example` → `<projeto>/.env.local` (stub com placeholders)
- gera stubs `<projeto>/src/app/api/auth/{request,validate,refresh,logout,me}/route.ts`
- instala `percus-auth ≥0.3.0` via pip ou npm
- não acessa auth-service (não armazena nem solicita TOTP step-up)

**Checklist humano** (no `COMANDO_PROJETO_NOVO.md` + `UPGRADE_PARA_FASE7.md`):
- [ ] Audience criada via auth-service UI (`/admin/audiences/new`) com slug kebab-case e step-up TOTP
- [ ] Branding subido via `/admin/audiences/{slug}/branding` (logo, palette, product_name, copy)
- [ ] Smoke E2E: OTP request → validate → `/me` (3 chamadas, ver §6.3)

### 2.3 Audience naming → **kebab-case obrigatório + migração forçada**

R7 do canon ganha enforcement.

`UPGRADE_PARA_FASE7.md` inclui passo de migração:
1. SQL migration no auth-service renomeando audiences legadas (`plexco_tickets` → `plexco-tickets`)
2. Grep cross-repo nos consumidores procurando strings com underscore e abrindo PR de substituição
3. Regression test no plugin percus-review: falha se algum `.env*` ou code referenciar audience com underscore depois da v6.8

---

## 3. Componentes a entregar

### 3.1 Auth-service (`D:/Claud Automations/auth-service`)

Mudanças backend:

| Item | Tipo | Arquivo (provável) |
|---|---|---|
| Migration adicionando colunas em `auth.audiences` | Alembic | `services/api/alembic/versions/012_audience_branding.py` |
| **Não duplica `product_name`** — reusa `display_name` existente. Service layer mapeia `display_name` → `product_name` no response | service | `services/api/app/modules/tenants/service.py` |
| `logo_url TEXT NULL`, `logo_url_dark TEXT NULL` | colunas | mesma migration |
| `palette JSONB NULL` — `{ primary, accent, gradient? }` | coluna | mesma migration |
| `copy JSONB NULL` — `{ login_title?, helper_text?, cta? }` (fallback resolvido server-side com `{product_name}` interpolation) | coluna | mesma migration |
| `support_contact_url TEXT NULL` — destino do link "Falar com o operador" (WhatsApp, mailto, ou URL custom) | coluna | mesma migration |
| `alias_slugs TEXT[] NULL` — slugs antigos tolerados durante janela de 7 dias na migração de naming (§5) | coluna | mesma migration |
| `origins TEXT[] NOT NULL DEFAULT '{}'` — origens autorizadas (lookup de `/tenants/by-origin`). Ex: `{'https://coach.plexco.com.br','https://coach.vercel.app'}`. Unique constraint nos elementos do array. | coluna | mesma migration |
| Migration renomeando audiences legadas (`plexco_tickets` → `plexco-tickets`) | Alembic | `services/api/alembic/versions/0NN_audience_rename_underscore.py` |
| Endpoint `GET /tenants/by-origin` | rota pública | `services/api/app/modules/tenants/router.py` (novo módulo) |
| Endpoint `PUT /admin/audiences/{slug}/branding` (step-up TOTP) | rota admin | mesmo módulo |
| Rate limit: 100 req/min por origin no `/tenants/by-origin` | middleware | `services/api/app/core/rate_limit.py` (estender) |
| OpenAPI tag + exemplos | docs | gerado |

### 3.2 Canon (`_Novo_Projeto`)

| Item | Path | Ação |
|---|---|---|
| R7 — adicionar enforcement kebab-case + cross-link `PADRAO_AUTH_SERVICE_INTEGRATION_V2.md` | `01_REGRAS_INEGOCIAVEIS.md` | edit |
| R7 — referência ao endpoint `/tenants/by-origin` e ao schema branding | `01_REGRAS_INEGOCIAVEIS.md` | edit |
| `templates/login-ui/` — novo diretório (ver §4 estrutura) | `templates/login-ui/` | criar |
| `templates/MIGRATION_KIT_AUTH.template.md` — atualizar para R15 phone normalization + tenant detection C | edit | |
| `comandos/COMANDO_PROJETO_NOVO.md` — adicionar bloco "Auth setup" inline (scaffold script + checklist) | edit | |
| `comandos/UPGRADE_PARA_FASE6.md` → renomear para `UPGRADE_PARA_FASE7.md` ou adicionar `UPGRADE_PARA_FASE7.md` | criar/renomear | |
| `tools/scaffold-percus-project.ps1` (+ .sh) | `tools/` | criar |
| `CANON_VERSION.md` — entry v6.8.0 + breaking changes | edit | |
| `.percus-version` — bump 6.7.2 → 6.8.0 | edit | |

### 3.3 Plugin (`plugin/percus-review`)

| Item | Path | Ação |
|---|---|---|
| Bump versão para alinhar canon | `plugin.json`, `marketplace.json` | edit |
| Test regression: audiência com underscore em código → falha | `tests/audience-kebab-enforce.tests.ps1` | criar |
| Update sensitive_paths para detectar mudanças em `audiences/` migrations | `scripts/review-router.ps1` | edit |

### 3.4 Lib `percus-auth` (`auth-service/libs/...`)

Já em v0.3.0 com `phone_handle_for_db_lookup()`. **Adicionar v0.4.0:**

- `getTenantConfig(origin: string): Promise<TenantConfig>` (TypeScript) — wrapper do `GET /tenants/by-origin` com cache 5min sessionStorage.
- `useTenant()` React hook + `TenantProvider` context.
- Python: `tenant.fetch_config(origin: str) -> TenantConfig` (httpx).

---

## 4. Estrutura de `_Novo_Projeto/templates/login-ui/`

```
templates/login-ui/
├── README.md                      # uso: copy para projeto + configurar .env
├── components/
│   ├── login-card.tsx             # orquestrador (extraído Plexco Tasks request-step.tsx + parametrizado)
│   ├── method-toggle.tsx          # WhatsApp/Email toggle
│   ├── country-dial-picker.tsx    # BR/US/PT/AR (flags SVG inline + mask phone)
│   ├── tenant-provider.tsx        # context provider que chama useTenant() + cacheia
│   └── support-link.tsx           # "Problemas com o acesso? Falar com o operador"
├── api/
│   ├── request.ts.template        # POST /api/auth/request → percus-auth lib /otp/request
│   ├── validate.ts.template       # POST /api/auth/validate → set cookie httpOnly
│   ├── refresh.ts.template        # POST /api/auth/refresh
│   ├── logout.ts.template         # POST /api/auth/logout
│   └── me.ts.template             # GET /api/auth/me (proxy /me com cookie)
├── styles/
│   └── login.module.css           # base CSS com CSS variables para palette (--percus-primary, etc.)
└── .env.example                   # AUTH_SERVICE_URL, PERCUS_AUTH_AUDIENCE_FALLBACK, INTERNAL_AUTH_KEY (S2S)
```

Convenção: arquivos `.template` exigem substituição de `{{PROJECT_NAME}}` etc. pelo scaffold script. Arquivos sem `.template` são copiados literalmente.

`TenantProvider` é mounted no root layout antes do login card; chama `getTenantConfig(window.location.origin)` no mount e renderiza children com `<TenantContext.Provider value={...}>`. Sem renderizar nada até resolver (skeleton) ou usar fallback do env se falhar.

`LoginCard` consome `useTenant()` para `product_name`, `palette`, `copy.helper_text` (interpolação `{product_name}` resolvida client-side se copy não veio do tenant config).

---

## 5. Migração de audiences legadas

Detectado: `plexco_tickets` (underscore) em uso por Plexco Tasks. Lookup atual do incidente.

### Plano de migração

1. **Auth-service Alembic** — `0NN_audience_rename_underscore.py`:
   ```sql
   UPDATE auth.audiences SET slug = 'plexco-tickets' WHERE slug = 'plexco_tickets';
   -- Repetir para cada audience legada identificada via:
   SELECT slug FROM auth.audiences WHERE slug LIKE '%\_%' ESCAPE '\';
   ```

2. **Consumidores** — para cada projeto Percus:
   - grep `'plexco_tickets'`, `"plexco_tickets"` em `.env*`, `.py`, `.ts`, `.tsx`, configs YAML
   - substituir por `plexco-tickets`
   - rebuild + smoke E2E

3. **Janela de coordenação** — auth-service migration roda PRIMEIRO; old slug fica em alias temporário (`alias_slugs TEXT[]`) por 7 dias para tolerar projetos consumidores em diferentes momentos de update. Após 7 dias remove alias.

4. **Regression test** — `plugin/percus-review/tests/audience-kebab-enforce.tests.ps1`:
   - Grep no working tree por padrões `(plexco|familia|painel|paid)_[a-z]+` em audience contexts (env vars `PERCUS_AUTH_AUDIENCE`, JWT aud claim hardcoded, etc.)
   - Falha se encontrado.

---

## 6. Upgrade path

### 6.1 Projeto novo (`COMANDO_PROJETO_NOVO.md`)

Bloco "Auth setup" inline, após scaffold inicial do Next.js/FastAPI:

```bash
# 1. Scaffold mecânico (script idempotente)
pwsh "$env:PERCUS_CANON_DIR/tools/scaffold-percus-project.ps1" `
    -ProjectPath "<path>" `
    -AudienceFallback plexco-coach

# 2. Checklist humano (CHECKLIST_AUTH.md gerado pelo script no projeto)
# - [ ] Criar audience no auth-service UI (/admin/audiences/new, slug kebab-case)
# - [ ] Subir branding (/admin/audiences/{slug}/branding) — logo, palette, copy
# - [ ] Smoke E2E (rodar tools/smoke-auth.ps1 do projeto)
```

### 6.2 Projeto existente (`UPGRADE_PARA_FASE7.md`)

Passos novos vs `UPGRADE_PARA_FASE6.md`:

1. **Bump canon** — atualizar `.percus-version`, plugin, et al. para 6.8.0
2. **Audit audience naming** — rodar grep + abrir PR se houver underscore
3. **Bump `percus-auth` lib** — para ≥0.3.0 (phone normalization R15) ou ≥0.4.0 (tenant hooks)
4. **Migrar login UI** — se existe `<projeto>/src/app/login/`, fazer diff vs `templates/login-ui/` e refatorar (opcional na primeira passada para projetos com login já estável; obrigatório para projetos com bug ou drift)
5. **Configurar tenant config** — via UI auth-service, subir `product_name`, `logo_url`, `palette`, `copy`
6. **Smoke E2E** — validar tenant detection + flow OTP completo

### 6.3 Smoke E2E (referencia `tools/smoke-auth.ps1`)

Script criado pelo scaffold no projeto. Roda:

```
1. curl https://auth.huboperacional.com.br/tenants/by-origin?origin=$(window.location.origin)
   → espera 200 com { audience, product_name, logo_url, palette }
2. curl POST /otp/request com destination + audience
   → espera 200 com challenge_id
3. (manual) operador insere OTP recebido
   curl POST /otp/validate com challenge_id + code
   → espera 200 com set-cookie httpOnly
4. curl GET /me com cookie
   → espera 200 com identity + memberships
```

---

## 7. Verificação end-to-end

1. **Tenant detection** — request a `GET /tenants/by-origin?origin=https://coach.huboperacional.com.br` retorna `{ audience: "plexco-coach", product_name: "Plexco Coach", ... }`.
2. **Rate limit funcional** — 101 requests em 60s do mesmo origin → 429.
3. **Cache frontend** — primeiro mount chama auth-service; segundo mount em <5min lê do sessionStorage (Network tab vazia).
4. **Login template parametrizado** — copiando `templates/login-ui/` para projeto novo + setando audience fallback, render mostra "Entrar no Plexco Coach" sem hardcode.
5. **Audience migration** — após Alembic rename, query `SELECT slug FROM auth.audiences WHERE slug LIKE '%\_%' ESCAPE '\'` retorna 0 rows.
6. **Regression test do plugin** — grep cross-repo por audience com underscore retorna 0 matches; teste passa.
7. **Smoke E2E completo** — flow OTP → validate → `/me` funciona no projeto recém-scaffoldado sem código manual além do checklist.
8. **Lib `percus-auth` 0.4.0** — `getTenantConfig()` retorna tipo correto; cache válido; falha graceful retorna fallback.

---

## 8. Out of scope (deliberadamente fora)

- **Skill `/percus-review:version`** (changelog visível por comando) — adiado desde v6.7.2; ainda não tem incidente recorrente.
- **Multi-tenant access switcher pós-login** — usuário com acesso a N tenants vê todos depois de logado e escolhe. Padrão Google Workspace. Fica para v6.9+; primeiro precisa dos N tenants existirem.
- **SSO multi-domain spec completa** (TTL código, CSRF) — incluído nos doc gaps de R7 mas implementação detalhada fica para sprint própria (auth-service tem rota `/sso` mas spec falta).
- **PII sanitization audit** — verificar implementação `message_audit/sanitize.py` cumpre R14 spec. Sprint própria de observabilidade.
- **Login com Google/social** — não está no padrão Percus; magic-link é o método canônico.

---

## 9. Próximos passos

1. Operador revisa este spec.
2. Após aprovação, invocar `superpowers:writing-plans` para gerar plano executável com tasks numeradas.
3. Pipeline R2: `[0] design → [1] spec → [2] execução → [3] handoff → [4] code complete → [5] tested`.
4. Sprint deve ser dividida em frentes paralelas com `superpowers:subagent-driven-development`:
   - **Frente A** — auth-service (migration + endpoint + admin route)
   - **Frente B** — templates/login-ui/ (extração Plexco Tasks + parametrização)
   - **Frente C** — canon docs + comandos + scaffold script
   - **Frente D** — lib percus-auth v0.4.0 (tenant hooks)
   - **Frente E** — migration audiences legadas (coordenada com Frente A)
