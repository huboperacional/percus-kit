Confirmation and validation live next to the control that caused them — this system has no toasts.

```jsx
<StatusLine state={status}>{message || 'Paste the link from the Share button.'}</StatusLine>
```
