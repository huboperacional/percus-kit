Icon-only action — pagination chevrons, close, overflow. Always pass `label`.

```jsx
<IconButton label="Previous section"><ChevronLeft /></IconButton>
```

Keep at 40px on desktop, 44px on touch surfaces.
