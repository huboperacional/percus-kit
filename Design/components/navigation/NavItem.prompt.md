Use inside Sidebar. The count pill is how a user judges where the work is — include it whenever the destination holds a countable set.

```jsx
<NavItem label="Window Tint" count={5} active={id === 'tint'} onClick={() => go('tint')} />
```
