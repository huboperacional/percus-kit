Whenever the image carries the meaning — galleries, asset pickers, media managers. Never shrink the thumbnail below --card-min; a small thumbnail is a broken thumbnail.

```jsx
<MediaCard src={url} title="Gallery photo 3" hint="Home · Gallery"
  badge={<Pill tone="overlay" uppercase>Before</Pill>}
  actions={<Button size="sm" icon={<Upload />}>Change</Button>}
  status={<StatusLine state="saved">Saved</StatusLine>} />
```
