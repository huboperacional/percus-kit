Every view opens with one. Keep the description to a single line of 13px tertiary ink.

```jsx
<PageHeader badge={<Pill tone="accent" uppercase>Section 01 / 07</Pill>} meta="26 editable fields"
  title="Home" description="The landing page — brand marks, hero video and the main gallery."
  actions={<><IconButton label="Previous"><ChevronLeft /></IconButton><Button iconRight={<ChevronRight />}>Next</Button></>} />
```
